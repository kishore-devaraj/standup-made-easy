'use strict'

window.addEventListener('load', () => {
    const app = document.getElementById('app')
    const homeTemplate = document.getElementById('main-page').innerHTML
    


    app.innerHTML = homeTemplate
})